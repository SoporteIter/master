/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { RoutePipe } from './route.pipe';

describe('Pipe: Route', () => {
  it('create an instance', () => {
    let pipe = new RoutePipe();
    expect(pipe).toBeTruthy();
  });
});
